# Copy to .env to override docker-compose's outbound (host-side) ports.
# Values shown are the defaults used when unset.

DATABASE_PORT=5431
BACKEND_PORT=8002
MAILHOG_UI_PORT=8027
MAILHOG_SMTP_PORT=1027
